package AdapterPattern.sample1.shapes

class Rectangle {
    fun draw(x1: Int, y1: Int, width: Int, height: Int) {
        println("Start from Point : ($x1 , $y1) and scale with width : $width and height : $height")
    }
}